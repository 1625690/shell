#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <unistd.h>

int main(int argc, char *argv[]){
	

	char wt[] = "ls -la | wc -l";
	char* arg[2];
	char* token;
	token = strtok();
	for(int i = 0; token != NULL; i++){
		arg[i] = 
	}
	return 0;
}